{
  "name": "opportunity-extension",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/background.ts src/bridge.ts src/popup.ts --bundle --outdir=dist --format=esm --target=chrome100 && cp public/manifest.json public/popup.html dist/",
    "dev": "esbuild src/background.ts src/bridge.ts src/popup.ts --bundle --outdir=dist --format=esm --target=chrome100 --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.0"
  }
}
