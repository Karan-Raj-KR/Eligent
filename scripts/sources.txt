# Freeform listing pages for discover.ts, one per line (# comments ignored).
#
# All active sources now live in scripts/sources.ts as config objects — each
# with its own detail-page pattern — because the generic anchor heuristic pulls
# in hundreds of non-detail links from index pages like
# https://www.indiascholarships.in/scholarships . Add a line here only for a
# one-off listing page that does not warrant a sources.ts entry.
